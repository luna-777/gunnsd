Put images here.

gunn-logo.png  -> the Gunn logo used in the site header (transparent PNG or SVG;
                  roughly 200x80px works well). If this file is missing, the
                  header falls back to a red "G" monogram, so nothing breaks.
favicon.png    -> 32x32 or 48x48 browser tab icon.
officers/      -> officer headshots, cropped to 4:5 portrait (~800x1000px).
